# PasTele legacy redirects that are not handled by Pages Functions.
# Public pretty routes (/c/*, /pf/*, /pp/*, /p/*, /gf/*, /gp/*, /ch/*)
# are handled by /functions so the original pathname/slug is preserved.
