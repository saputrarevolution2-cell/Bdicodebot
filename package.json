{
  "name": "telecod-cloudflare-pages",
  "private": true,
  "version": "1.0.1",
  "scripts": {
    "dev": "wrangler pages dev .",
    "deploy": "wrangler pages deploy . --project-name telecod"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
