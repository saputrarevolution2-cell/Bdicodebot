(async()=>{const el=document.getElementById('adminContent');try{await Admin.guard();const data=Admin.rows(await Admin.rpc('admin_products',{p_limit:200,p_offset:0}));el.innerHTML=`<div class="pt-toolbar"><input id="pid" class="input" placeholder="Product UUID"><select id="pstatus" class="select"><option value="published">published</option><option value="active">active</option><option value="draft">draft</option><option value="rejected">rejected</option><option value="hidden">hidden</option></select><input id="pprice" class="input" type="number" min="0" step="100" placeholder="Harga"><button class="btn primary" id="update"><i class="fa-solid fa-pen"></i> Update</button><button class="btn danger" id="remove"><i class="fa-solid fa-trash"></i> Hapus</button><button class="btn" id="refresh"><i class="fa-solid fa-rotate"></i> Refresh</button></div><div id="data"></div>`;data.innerHTML=Admin.table(data);update.onclick=async()=>{try{const id=pid.value.trim();if(!id)throw new Error('Masukkan Product UUID');await Admin.rpc('admin_update_product',{p_id:id,p_status:pstatus.value,p_price:Number(pprice.value||0)});TC.toast('Product diperbarui','success');location.reload()}catch(e){TC.toast(e.message,'error')}};remove.onclick=async()=>{try{const id=pid.value.trim();if(!id)throw new Error('Masukkan Product UUID');if(!confirm('Hapus product ini?'))return;await Admin.rpc('admin_delete_product',{p_id:id});TC.toast('Product dihapus','success');location.reload()}catch(e){TC.toast(e.message,'error')}};refresh.onclick=()=>location.reload()}catch(e){el.innerHTML='<div class="empty">'+Admin.esc(e.message)+'</div>'}})();