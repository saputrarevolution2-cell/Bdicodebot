PasTele Admin — Akses Ditolak FINAL FIX
1. Run admin/ADMIN-ACCESS-FINAL-FIX.sql once in the Supabase SQL Editor.
2. Deploy the entire admin/ folder from this ZIP.
3. Logout from telecod.biz.id, login again with the intended admin account, then open /admin/.
4. The authenticated account must have profiles.username='admin', is_admin=true OR role='admin'/'owner', and is_banned=false.
5. SQL Editor SELECT results for auth.uid() are not a browser-session test.
