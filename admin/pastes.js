(async()=>{try{await Admin.guard();const data=await Admin.rpc("admin_pastes",{p_limit:200,p_offset:0});const rows=Array.isArray(data)?data:(data?[data]:[]);adminContent.innerHTML=`<div class="pt-toolbar"><input id="pasteId" class="input" placeholder="Paste UUID"><button class="btn danger" id="remove">Hapus Paste</button><button class="btn" id="refresh">Refresh</button></div><div id="data"></div>`;data.innerHTML=Admin.table(rows);remove.onclick=async()=>{const id=pasteId.value.trim();if(!id)return TC.toast('Masukkan Paste UUID','error');if(!confirm('Hapus paste ini?'))return;try{await Admin.rpc('admin_delete_paste',{p_id:id});TC.toast('Paste dihapus','success');location.reload()}catch(e){TC.toast(e.message,'error')}};refresh.onclick=()=>location.reload()}catch(e){adminContent.innerHTML='<div class="empty">'+TC.esc(e.message)+'</div>'}})()