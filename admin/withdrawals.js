(async()=>{const el=document.getElementById('adminContent');try{await Admin.guard();const data=Admin.rows(await Admin.rpc('admin_withdrawals',{p_limit:200,p_offset:0}));const pending=data.filter(x=>String(x.status||'').toLowerCase()==='pending').length;el.innerHTML=`<div class="admin-page-head"><div><span class="eyebrow">FINANCE</span><h2 style="margin:8px 0">Withdrawal Queue</h2><p>${pending} pending request${pending===1?'':'s'}.</p></div><div class="admin-kpi"><i class="fa-solid fa-clock"></i><b>${pending}</b><span>Pending</span></div></div><div class="pt-toolbar"><input id="wid" class="input" placeholder="Withdrawal UUID"><select id="wstatus" class="select"><option value="approved">approved</option><option value="completed">completed</option><option value="rejected">rejected</option><option value="cancelled">cancelled</option></select><input id="wnote" class="input" placeholder="Catatan admin"><button class="btn primary" id="process"><i class="fa-solid fa-check"></i> Proses</button><button class="btn" id="refresh"><i class="fa-solid fa-rotate"></i> Refresh</button></div><div id="data"></div>`;data.innerHTML=Admin.table(data);process.onclick=async()=>{try{const id=wid.value.trim();if(!id)throw new Error('Masukkan Withdrawal UUID');await Admin.rpc('admin_process_withdrawal',{p_id:id,p_status:wstatus.value,p_note:wnote.value.trim()||null});TC.toast('Withdrawal diproses','success');location.reload()}catch(e){TC.toast(e.message,'error')}};refresh.onclick=()=>location.reload()}catch(e){el.innerHTML='<div class="empty">'+Admin.esc(e.message)+'</div>'}})();