(() => {'use strict';const A=window.PasTeleAdmin,$=A.$;let ctl={};
async function controls(){const s=await A.call('get_public_site_settings')||{};ctl={withdrawal_instant:{enabled:s.withdrawal_instant?.enabled!==false,reason:s.withdrawal_instant?.reason||''},withdrawal_manual:{enabled:s.withdrawal_manual?.enabled!==false,reason:s.withdrawal_manual?.reason||''}};for(const [id,k] of [['instant','withdrawal_instant'],['manual','withdrawal_manual']]){const c=ctl[k];$('#'+id+'Badge').textContent=c.enabled?'AKTIF':'DITUTUP';$('#'+id+'Badge').className='control-status '+(c.enabled?'on':'off');$('#'+id+'Text').textContent=c.enabled?'Permintaan WD tersedia.':(c.reason||'Ditutup admin.');$('#'+id+'Btn').textContent=c.enabled?'Tutup WD '+(id==='instant'?'Instan':'Manual'):'Buka WD '+(id==='instant'?'Instan':'Manual');$('#'+id+'Btn').className='btn '+(c.enabled?'danger':'success');$('#'+id+'Reason').value=c.reason;$('#'+id+'Reason').classList.toggle('hidden',c.enabled)}}
async function toggle(k,id){const c=ctl[k];let reason='';if(c.enabled){reason=$('#'+id+'Reason').value.trim();if(!reason){A.toast('Isi alasan penutupan.','error');return}}else if(!confirm('Buka kembali fitur ini?'))return;try{await A.call('admin_set_platform_control',{p_section:k,p_enabled:!c.enabled,p_reason:reason});A.toast('Pengaturan diperbarui.');await load()}catch(e){A.toast(A.errText(e),'error')}}
async function load(){const box=$('#table');try{await controls();const data=await A.rpc('admin_withdrawals',{p_limit:300,p_offset:0});if(!data.length){box.innerHTML='<div class="empty"><i class="fa-regular fa-circle-check"></i><strong>Tidak ada withdrawal</strong></div>';return}A.table(box,data,[{label:'User',render:r=>`<strong>${A.esc(r.username||r.account_name||'-')}</strong><small>${A.esc(r.auth_email||r.account_number||r.user_id||'')}</small>`},{label:'Amount',render:r=>A.money(r.amount)},{label:'Fee',render:r=>A.money(r.fee)},{label:'Mode',key:'mode'},{label:'Method',key:'method'},{label:'Status',render:r=>`<span class="status">${A.esc(r.status||'-')}</span>`},{label:'Created',render:r=>r.created_at?new Date(r.created_at).toLocaleString('id-ID'):'-'}],r=>r.status==='pending'?`<button class="btn small success" data-wd="${A.esc(r.id)}" data-status="approved"><i class="fa-solid fa-check"></i> Approve</button> <button class="btn small" data-wd="${A.esc(r.id)}" data-status="completed"><i class="fa-solid fa-circle-check"></i> Complete</button> <button class="btn small danger" data-wd="${A.esc(r.id)}" data-status="rejected"><i class="fa-solid fa-xmark"></i> Rejected</button>`:'—')}catch(e){$('#error').className='admin-alert error';$('#error').textContent=A.errText(e);A.toast(A.errText(e),'error')}}
document.addEventListener('click',async e=>{const b=e.target.closest('[data-wd]');if(!b)return;let note='';if(b.dataset.status==='rejected'){note=prompt('Alasan penolakan WD (akan dikirim ke notifikasi user):','')??'';if(!note.trim()){A.toast('Alasan reject wajib diisi.','error');return}}else{note=prompt('Catatan admin (opsional):','')??''}try{b.disabled=true;await A.call('admin_process_withdrawal',{p_id:b.dataset.wd,p_status:b.dataset.status,p_note:note});A.toast('Withdrawal diperbarui.');await load()}catch(x){A.toast(A.errText(x),'error')}finally{b.disabled=false}});document.addEventListener('DOMContentLoaded',async()=>{if(await A.requireAdmin())load()});$('#instantBtn')?.addEventListener('click',()=>toggle('withdrawal_instant','instant'));$('#manualBtn')?.addEventListener('click',()=>toggle('withdrawal_manual','manual'));$('#refresh')?.addEventListener('click',load);
})();
