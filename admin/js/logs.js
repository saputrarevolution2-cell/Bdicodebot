(() => {'use strict';const A=window.PasTeleAdmin,$=A.$;async function load(){const box=$('#adminContent');A.setLoading(box);try{if(!await A.requireAdmin())return;const data=await A.rpc('admin_logs',{p_limit:300,p_offset:0});box.innerHTML=`<div class="admin-toolbar"><div class="toolbar-title"><i class="fa-solid fa-list-check"></i><div><strong>Audit Logs</strong><span>${data.length} aktivitas</span></div></div><button class="btn" id="reload">Refresh</button></div><div id="adminTable"></div>`;A.table($('#adminTable'),data,[{label:'Action',key:'action'},{label:'Admin',key:'admin_id'},{label:'Target',render:r=>`${A.esc(r.target_type||'-')}<small>${A.esc(r.target_id||'-')}</small>`},{label:'Details',render:r=>`<code>${A.esc(JSON.stringify(r.details||{}))}</code>`},{label:'Created',render:r=>r.created_at?new Date(r.created_at).toLocaleString('id-ID'):'-'}]);$('#reload').onclick=load}catch(e){box.innerHTML=`<div class="empty error-state">${A.esc(A.errText(e))}</div>`}}document.addEventListener('DOMContentLoaded',load)})();
