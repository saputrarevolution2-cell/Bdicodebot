(() => {'use strict';const A=window.PasTeleAdmin,$=A.$;async function load(){const box=$('#adminContent');A.setLoading(box);try{if(!await A.requireAdmin())return;const data=await A.rpc('admin_transactions',{p_limit:300,p_offset:0});box.innerHTML=`<div class="admin-toolbar"><div class="toolbar-title"><i class="fa-solid fa-arrow-right-arrow-left"></i><div><strong>Transactions</strong><span>${data.length} data</span></div></div><button class="btn" id="reload">Refresh</button></div><div id="adminTable"></div>`;A.table($('#adminTable'),data,[{label:'User',key:'user_id'},{label:'Type',key:'type'},{label:'Amount',render:r=>A.money(r.amount)},{label:'Fee',render:r=>A.money(r.fee)},{label:'Net',render:r=>A.money(r.net_amount)},{label:'Status',key:'status'},{label:'Created',render:r=>r.created_at?new Date(r.created_at).toLocaleString('id-ID'):'-'}]);$('#reload').onclick=load}catch(e){box.innerHTML=`<div class="empty error-state">${A.esc(A.errText(e))}</div>`}}document.addEventListener('DOMContentLoaded',load)})();
