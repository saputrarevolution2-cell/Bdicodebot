(() => {'use strict';const A=window.PasTeleAdmin,$=A.$;async function load(){const box=$('#adminContent');A.setLoading(box);try{if(!await A.requireAdmin())return;const data=await A.rpc('admin_payments',{p_limit:200,p_offset:0});box.innerHTML=`<div class="admin-toolbar"><div class="toolbar-title"><i class="fa-solid fa-credit-card"></i><div><strong>Payments</strong><span>${data.length} data</span></div></div><button class="btn" id="reload">Refresh</button></div><div id="adminTable"></div>`;A.table($('#adminTable'),data,[{label:'Payment',render:r=>`<strong>${A.esc(r.id)}</strong><small>Order: ${A.esc(r.order_id||'-')}</small>`},{label:'Amount',render:r=>A.money(r.amount)},{label:'Provider',key:'provider'},{label:'Status',render:r=>`<span class="status">${A.esc(r.status||'-')}</span>`},{label:'Reference',key:'reference'},{label:'Created',render:r=>r.created_at?new Date(r.created_at).toLocaleString('id-ID'):'-'}]);$('#reload').onclick=load}catch(e){box.innerHTML=`<div class="empty error-state">${A.esc(A.errText(e))}</div>`}}document.addEventListener('DOMContentLoaded',load)})();
