(async()=>{try{await Admin.guard();const data=await Admin.rpc("admin_logs",{p_limit:200,p_offset:0});const rows=Array.isArray(data)?data:(data?[data]:[]);adminContent.innerHTML=`<div class="pt-toolbar"><button class="btn" id="refresh">Refresh</button></div><div id="data"></div>`;data.innerHTML=Admin.table(rows);refresh.onclick=()=>location.reload()}catch(e){adminContent.innerHTML='<div class="empty">'+TC.esc(e.message)+'</div>'}})()