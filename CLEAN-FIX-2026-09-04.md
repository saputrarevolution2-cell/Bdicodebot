# PasTele Clean UI Fix — 2026-09-04

## What was fixed
- Consolidated duplicated shared CSS into `css/app.css`.
- Consolidated duplicated page CSS into one canonical CSS file per page.
- Removed duplicated/legacy CSS variants that were no longer referenced.
- Consolidated duplicated page JS into one canonical JS file per page.
- Removed the duplicated footer/page-shell code embedded inside page scripts. `js/site-shell.js` is now the single footer shell.
- Admin JS utility is now loaded once from `js/admin/admin.js` and each admin page loads only its own page script.
- Added Font Awesome to every HTML page, including auth callback and reset password.
- Added final dark/light theme normalization for cards, forms, tables, notices, buttons, footer and common UI elements.
- Admin panel links now point to `admin/index.html`.
- The Settings Admin button is a real link and is shown only to admin users.
- Profile Admin button remains a real link and is shown only to admin users.
- Kept Supabase/auth/database logic intact; cleanup is focused on asset duplication, UI consistency, theme behavior and navigation.

## Canonical structure
- Shared CSS: `css/app.css`
- One page CSS per HTML page in `css/`
- Shared JS: `theme-preload.js`, `config.js`, `supabase.js`, `auth.js`, `navbar.js`, `site-shell.js`, `debug-panel.js`
- One page JS per HTML page in `js/`
- Admin shared JS: `js/admin/admin.js`
- Admin page JS: one file per admin page in `js/admin/`

## Validation
- JavaScript syntax checked with Node `--check` for all remaining JS files.
- HTML asset references reviewed after cleanup.
- Legacy capitalized page asset references removed from HTML.
- Duplicate embedded page-shell/footer blocks removed from page scripts.
