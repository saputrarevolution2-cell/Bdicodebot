
window.TC_I18N={id:{back:"Marketplace",buy:"Beli Sekarang",notFound:"Produk tidak ditemukan.",login:"Login diperlukan untuk membeli produk.",category:"Kategori",delivery:"Delivery",digital:"Konten digital",channel:"Telegram Channel",external:"External link"},en:{back:"Marketplace",buy:"Buy Now",notFound:"Product not found.",login:"Login is required to buy this product.",category:"Category",delivery:"Delivery",digital:"Digital content",channel:"Telegram Channel",external:"External link"}};
const sb=supabase.createClient(TELECOD_CONFIG.SUPABASE_URL,TELECOD_CONFIG.SUPABASE_ANON_KEY),slug=new URLSearchParams(location.search).get("slug");
let currentProduct=null;
function deliveryName(v){return v==="telegram_channel"?tcT("channel"):v==="external"?tcT("external"):tcT("digital")}
async function load(){const view=document.getElementById("view");const {data,error}=await sb.from("products").select("id,title,description,price,category,thumbnail_url,seller_id,delivery_type").eq("slug",slug).eq("status","published").single();if(error||!data){view.innerHTML=`<div class="auth-card"><div class="error">${tcT("notFound")}</div></div>`;return}currentProduct=data;view.innerHTML=`<div class="visual">${data.thumbnail_url?`<img src="${tcEscape(data.thumbnail_url)}" alt="${tcEscape(data.title)}">`:'<i class="fa-solid fa-code fa-4x"></i>'}</div><div class="product-info"><span class="kicker">${tcEscape(data.category||"Digital")}</span><h1>${tcEscape(data.title)}</h1><p class="description">${tcEscape(data.description||"")}</p><div class="product-price">${tcMoney(data.price)}</div><div class="product-meta"><span class="meta-chip">${tcT("category")}: ${tcEscape(data.category||"Digital")}</span><span class="meta-chip">${tcT("delivery")}: ${tcEscape(deliveryName(data.delivery_type))}</span></div><div class="buy-row"><button class="btn primary xl" id="buy"><i class="fa-solid fa-cart-shopping"></i>${tcT("buy")}</button><a class="btn ghost xl" href="marketplace.html">${tcT("back")}</a></div><div id="msg" style="margin-top:15px"></div></div>`;document.getElementById("buy").onclick=buy}
async function buy(){const {data:{user}}=await sb.auth.getUser();if(!user){location.href="login.html?next="+encodeURIComponent(location.href);return}const {data,error}=await sb.rpc("create_order",{p_product_id:currentProduct.id});if(error){document.getElementById("msg").innerHTML=`<div class="error">${tcEscape(error.message)}</div>`;return}const order=Array.isArray(data)?data[0]:data;location.href="checkout.html?id="+encodeURIComponent(order.id)}
document.addEventListener("DOMContentLoaded",load);window.addEventListener("telecod:language",load);
