document.addEventListener('DOMContentLoaded',async()=>{let u=await TC.user();if(!u)return location.href='login.html';let {data}=await sb.from('notifications').select('*').eq('user_id',u.id).order('created_at',{ascending:false});content.innerHTML=(data||[]).map(x=>`<div class="list-item"><span><b>${TC.esc(x.title)}</b><br><small class="muted">${TC.esc(x.message)}</small></span><small>${new Date(x.created_at).toLocaleString('id-ID')}</small></div>`).join('')||'<div class="empty">Tidak ada notifikasi.</div>'})