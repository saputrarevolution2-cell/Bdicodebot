document.addEventListener('DOMContentLoaded',async()=>{let u=await TC.user();if(!u)return location.href='login.html';let {data}=await sb.from('products').select('*').eq('creator_id',u.id).order('created_at',{ascending:false});content.innerHTML=(data||[]).map(x=>`<div class="list-item"><span><b>${TC.esc(x.title)}</b><br><small class="muted">${TC.esc(x.status)} · ${TC.money(x.price)}</small></span></div>`).join('')||'<div class="empty">Belum ada produk.</div>'})