document.addEventListener("DOMContentLoaded",async()=>{let slug=new URLSearchParams(location.search).get("slug");let {data,error}=await sb.from("pastelinks").select("*").eq("slug",slug).maybeSingle();if(error||!data)return pasteContent.innerHTML="<div class=empty>Paste tidak ditemukan.</div>";if(data.expires_at&&new Date(data.expires_at)<new Date())return pasteContent.innerHTML="<div class=empty>Paste sudah expired.</div>";pasteContent.innerHTML=`<span class="badge">${TC.esc(data.visibility)}</span><h1>${TC.esc(data.title)}</h1><div class="rich-output">${data.content_html}</div><p class="muted">${(data.tags||[]).map(x=>"#"+TC.esc(x)).join(" ")}</p>`;await sb.rpc("increment_paste_view",{p_slug:slug}); await sb.rpc("record_content_view",{p_owner:data.user_id,p_target_type:"link",p_target_id:data.id})})