document.addEventListener('DOMContentLoaded',async()=>{let slug=new URLSearchParams(location.search).get('slug');let q=slug?await sb.from('marketplace_public').select('*').eq('slug',slug).maybeSingle():{data:null};if(q.data)content.innerHTML=`<h2>${TC.esc(q.data.title)}</h2><p class="muted">${TC.esc(q.data.description||'')}</p><div class="notice">${TC.esc(q.data.content||'')}</div>`;else content.innerHTML='<div class="empty">Produk tidak ditemukan.</div>'})