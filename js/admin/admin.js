window.Admin={guard:async()=>{let p=await TC.profile();if(!p||(!p.is_admin&&p.role!=='admin')){location.href='../index.html';throw Error('Admin only')}return p},rpc:async(name,args={})=>{let {data,error}=await sb.rpc(name,args);if(error)throw error;return data},table:rows=>{if(!rows?.length)return '<div class="empty">Tidak ada data.</div>';let keys=Object.keys(rows[0]);return '<div class="table-wrap"><table class="table"><thead><tr>'+keys.map(k=>`<th>${TC.esc(k)}</th>`).join('')+'</tr></thead><tbody>'+rows.map(r=>'<tr>'+keys.map(k=>`<td>${TC.esc(typeof r[k]==='object'?JSON.stringify(r[k]):r[k])}</td>`).join('')+'</tr>').join('')+'</tbody></table></div>'}};